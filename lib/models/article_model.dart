class ArticleModel {
  String title, desc, content, url, urltoImg;

  ArticleModel(
      {required this.content,
      required this.desc,
      required this.title,
      required this.url,
      required this.urltoImg});
}
